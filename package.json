{
  "name": "mini-inventory-system",
  "version": "1.0.0",
  "description": "Mini Inventory Management System — monorepo with NestJS backend and React frontend",
  "private": true,
  "scripts": {
    "backend": "docker compose up -d --wait && cd backend && pnpm install && pnpm run db:push && pnpm run start:dev",
    "frontend": "cd frontend && pnpm install && pnpm run dev",
    "docker:up": "docker compose up -d --wait",
    "docker:down": "docker compose down"
  }
}
